<% Spine   = require('spine') %>

<% Cliente = require("models/cliente")  %>
<% cliente = Cliente.exists(@Cliente)  %>

<tr>
  <td>
    <a target="_blank" href="<%= Spine.session.instance_url + "/" + @id %>">
       <%= @Consecutivo || "No Impreso :" + @Referencia %>
    </a> 
  </td>
  <td>
    <a target="_blank" href="<%= Spine.session.instance_url + "/" + @id %>">
      <%= @Tipo_de_Documento %>
    </a>
  </td>
 
  <td><%= cliente?.Name %></td>
  
  <td><%= @Total.toMoney() %></td>
  <td><%= @Estado %></td>

 <td><a data-type="Documento" data-id="<%= @id %>" class="anularDocumento btn btn-info"><i class="icon-print icon-white"> </i> Anular</a></td>

</tr>